import "package:flutter/material.dart";

class CafetariaScreen extends StatelessWidget {
  const CafetariaScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Cafetaria"),)
    );
  }
}